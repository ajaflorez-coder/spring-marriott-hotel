# SQL SERVER
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/marriot
    username: root
    password: toor
  jpa:
    database: mysql
    hibernate:
      ddl-auto: update
    properties:
      dialect: org.hibernate.dialect.MYSQL5Dialect
    show-sql: true